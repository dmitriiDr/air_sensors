from setuptools import setup, find_packages

setup(
    name="my_hdf5",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "numpy",
        "pandas"
    ],
    author="Dmitrii Druzhbin",
    description="",
    url="",
)